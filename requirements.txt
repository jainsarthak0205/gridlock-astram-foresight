# Pinned to the versions the models were trained/serialized with, so the deployed
# app loads the .joblib artifacts without version-mismatch issues.
# (uv is the source of truth locally via pyproject.toml + uv.lock; this file is for
#  Streamlit Community Cloud, which installs from requirements.txt.)
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
lightgbm==4.6.0
pyarrow==24.0.0
joblib==1.5.3
scipy==1.17.1
streamlit==1.58.0
pydeck==0.9.2
